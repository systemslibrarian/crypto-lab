# GENERATED by tools/teach-build.js. Edit the form there, then rerun it.
name: Teaching material problem
description: A worksheet step that no longer matches its exhibit, an error in a module page, or an accessibility barrier.
title: "[Teaching material] "
labels: ["bug"]
body:
  - type: dropdown
    id: kind
    attributes:
      label: What kind of problem
      options:
        - "A worksheet step no longer matches its exhibit"
        - "An error in a module page or worksheet"
        - "Accessibility barrier"
        - "Privacy concern"
        - "Something else"
    validations:
      required: true
  - type: input
    id: page
    attributes:
      label: Page or exhibit
      description: The address of the teaching page or exhibit.
    validations:
      required: true
  - type: dropdown
    id: module
    attributes:
      label: Module
      options:
        - "Security & symmetric encryption"
        - "Public-key & signatures"
        - "Key exchange & secure channels"
        - "Post-quantum transition"
        - "Library privacy, for iSchools"
        - "Not about a module"
  - type: input
    id: environment
    attributes:
      label: Browser, device and assistive technology
      description: For example the browser and its version, and a screen reader or magnifier if you use one.
  - type: textarea
    id: what
    attributes:
      label: What happened
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: What you expected
